using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoyUnScript : MonoBehaviour
{
    // Start is called before the first frame update
    public bool _soyBool;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
